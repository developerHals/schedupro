// Backend-backed status updater is disabled. The app now runs locally and
// does not require authentication or database initialization on boot.
export const checkAndUpdateCourseStatuses = async () => {
  return 0;
};
